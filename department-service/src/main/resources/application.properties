spring.application.name=department-service
server.port=8080

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/department_db?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# eureka
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/

#kafka
# Kafka bootstrap server
spring.kafka.bootstrap-servers=kafka:9092

# (Optional) Consumer config, if you also want to consume
spring.kafka.consumer.group-id=department-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer